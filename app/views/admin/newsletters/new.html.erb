<% render :partial => 'admin/shared/mailing_sub_menu' %>

<h1><%= t("add_newsletter") %></h1>

<% form_for [:admin, @newsletter] do |f| %>
  <%= render :partial => f %>
  <%= f.submit t("create") %>
<% end %>

<%= link_to t('back'), admin_newsletters_path %>


